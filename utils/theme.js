import { createTheme } from "@material-ui/core";

const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#4e50c7",
    },
    secondary: {
      main: "#616f75",
    },
  },
});
export default theme;
