import { makeStyles } from "@material-ui/core";

const useStyels = makeStyles({
  title: {
    marginTop: 80,
  },
  main: {
    minHeight: "80vh",
  },
  footer: {
    textAlign: "center",
    margin: "20px 0px",
  },
});
export default useStyels;
